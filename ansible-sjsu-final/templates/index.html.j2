<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>SJSU {{ sjsu_id }}</title>
    <style>
      body { font-family: Arial, Helvetica, sans-serif; margin: 3rem; }
      .card { max-width: 720px; border: 1px solid #ddd; border-radius: 12px; padding: 24px; }
      h1 { margin-top: 0; }
      .muted { color: #666; }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>Hello World from SJSU-{{ sjsu_id }}</h1>
      <p class="muted">Served by Nginx on port 8080.</p>
    </div>
  </body>
</html>
